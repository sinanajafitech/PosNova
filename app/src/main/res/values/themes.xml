<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Compose owns all UI theming (see core/theme/Theme.kt);
         this is only the pre-Compose window theme used while the activity starts. -->
    <style name="Theme.PosNova" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
